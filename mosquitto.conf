# Listen on all interfaces inside the container, port 1883
listener 1883 0.0.0.0

# (Optional) Websockets listener
# listener 9001 0.0.0.0
# protocol websockets

# Allow anonymous connections for local dev (remove in prod)
allow_anonymous true

# Keep logs visible in container output
log_type all
